export default class Calculator {

    // A class can have only one constructor. If the class extends another class,
    // it can call `super()` to invoke the parent class's constructor.
    constructor() {
    }

    add(a, b) {
        return a + b;
    }
    subtract(a, b) {
        return a - b;
    }
}